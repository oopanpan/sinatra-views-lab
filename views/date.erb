<!DOCTYPE html>
    <html>
        <head>
        </head>
        <body>
            <h1>Today</h1>
            <% date = DateTime.now %>
            <p><%= "The date is " + date.strftime("%A, %B %d, %Y") %></p>
        </body>
    </html>